{{#if cumulativeDifferenceAlert}}
	```
	{{#if isHigher}}
		Attention!! Spike in metrics. \n
		{{#each dataset}}
			{{formatUnixTime value=time offset="-420" format="YYYY-MM-DD HH:mm"}}
			- {{value}}\n
		{{/each}}\n
		Difference: {{difference}}
	{{/if}}
	{{#if isLower}}
		Attention!! Dip in metrics. \n
		{{#each dataset}}
			{{formatUnixTime value=time offset="-420" format="YYYY-MM-DD HH:mm"}}
			- {{value}}\n
		{{/each}}\n
		Difference: {{difference}}
	{{/if}}
	```
{{/if}}
{{#if consistentVariationAlert}}
	```
	{{#if isLower}}
		Attention!! variation in metrics around {{time}} \n
		{{#each dataset}}
			{{this}}
			{{#if @last}}
			{{else}}, 
			{{/if}}
		{{/each}}
		Variation %: {{perct}}
	{{/if}}
	```
{{/if}}
