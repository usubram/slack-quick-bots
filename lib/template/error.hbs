{{#if invalidCommand}}
	```Make sure to send something like \n `<@{{botName}}> schedule <commandName> (*/15 * * * *)`\n
	Above example schedules <commandName> every 15 minutes. \n
	`<@{{botName}}> schedule list` lists all scheduled commands.```
{{else}}
	{{#if invalidCron}}
		*Invalid schedule command*.\n Make sure to send something like `schedule commandName (*/15 * * * *).`\n
		The above example schedules _commandName_ every 15 minutes.
	{{else}}
		{{#if restrictedUser}}
			*Attention!!* \n
			I'm configured to only respond to\n
			{{#each users}}
				<@{{this}}>\n
			{{/each}}
		{{else}}
			Howdy! here is some help:- \n\n
			{{#each command}}
				{{#if failedParams}}
					I found {{noOfErrors}} {{toPlural len=noOfErrors word="issue"}} in your request. \n
				{{/if}}
				{{#if inContext}}
					{{#if ../prefix}} <@{{../prefix}}> {{/if}}*{{toLowerCase @key}}* 
					{{#if upperLimit}}
						{{lowerLimit}} to {{{upperLimit}}} 
					{{/if}}
					{{#if paramsHelpMessage}}
						{{#each paramsHelpMessage}}
							{{toLowerCase sample}} 
						{{/each}}\n
						Usage: {{toLowerCase @key}} 
						{{#each paramsHelpMessage}}
							{{toLowerCase recommend}} 
						{{/each}}\n
					{{else}}
						{{#if allowedParam}}
							{{#each allowedParam}}
								{{#if ../mParams}}
									[ {{joinArray this seperator=" | "}} ] {{#if @last}} {{else}}  {{/if}}
								{{else}}
									{{toLowerCase .}}{{#if @last}} {{else}} | {{/if}} 
								{{/if}}
							{{/each}}\n
						{{/if}}
					{{/if}}
					{{#if failedParams}}
						{{#each failedParams}}
							 - {{toLowerCase error}} \n
						{{/each}}
						{{#if recommend}}
							`{{#if ../prefix}} <@{{../prefix}}> {{/if}}{{toLowerCase @key}}
							{{#if sampleParams}}
								 {{joinArray sampleParams seperator=" "}}
							{{/if}}` should work! \n
						{{/if}}
					{{/if}}\n
				{{else}}
					{{#if errorContext}}
						{{#if upperLimit}}
							{{lowerLimit}} to {{{upperLimit}}} 
						{{/if}}
						{{#if failedParams}}
							{{#each failedParams}}
								 - {{toLowerCase error}} \n
							{{/each}}
						{{else}}
							{{#if recommend}}
								`{{#if ../prefix}} <@{{../prefix}}>{{/if}} {{toLowerCase @key}} 
								{{#each recommend}}
									{{toLowerCase .}}{{#if @last}} {{else}} {{/if}} 
								{{/each}}`\n
							{{else}}
								{{#if allowedParam}}
									{{#each allowedParam}}
										{{#if ../mParams}}
											[ {{joinArray this seperator=" | "}} ] {{#if @last}} {{else}}  {{/if}}
										{{else}}
											{{toLowerCase .}}{{#if @last}} {{else}} | {{/if}} 
										{{/if}}
									{{/each}}\n
								{{/if}}
							{{/if}}
						{{/if}}\n
					{{else}}
						{{#if ../prefix}} <@{{../prefix}}> {{/if}}*{{toLowerCase @key}}* 
						{{#if aLimit }}
						 - _Usage: {{#if ../prefix}}<@{{../prefix}}>{{/if}} {{toLowerCase name}} {{{toLowerCase aLimit}}}_ \n
						{{/if}}
						{{#if alertContext}}
						 - _Usage: {{#if ../prefix}}<@{{../prefix}}>{{/if}} {{name}} setup <threshold>_ \n
						{{/if}}
						{{#if aParams}}
							- _Usage: {{#if ../prefix}}<@{{../prefix}}>{{/if}} {{toLowerCase name}} 
							{{#if mParams}}
									{{#each aParams}}
										{{toLowerCase .}} 
									{{/each}}
							{{else}}
								{{toLowerCase aParams}}
							{{/if}}_ \n
						{{/if}}\n
					{{/if}}
				{{/if}}
				{{#if helpText}}
					{{{helpText}}} \n
				{{/if}}\n
			{{/each}}\n
		{{/if}}
	{{/if}}
{{/if}}
